## Bugs
    1. accounts duplicate when data synced from YNAB multiple times

## Features
    1. rename sync from YNAB to something that sounds less like it's going to change things in my budget. Maybe "Import Accounts / Categories / Data"
    2. increase resolution so receipt images can be taken from further away with less stress over alignment of the image. 
    3. optional "debug" mode that shows realtime bounding boxes in the camera UI of which fields are being recognized
    4. optional "telemetry" mode that submits receipt images, along with the submitted metadata that may have been corrected (payee, total, date, account, etc)
    5. self improving receipt image OCR based on user submitted and corrected data
